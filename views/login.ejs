<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Roboto', sans-serif;
  background: linear-gradient(135deg, #74ebd5, #acb6e5);
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
}

.login-selection {
  text-align: center;
  background-color: white;
  padding: 40px 20px; /* Add some padding on the sides */
  border-radius: 15px;
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
  width: 90%;
  max-width: 500px; /* Adjusted max-width for better layout */
}

h2 {
  margin-bottom: 20px;
  color: #333;
  font-size: 1.8rem;
}

.button-group {
  display: flex; /* Use flexbox to arrange buttons in a row */
  justify-content: space-between; /* Space between buttons */
  gap: 20px; /* Space between buttons */
}

.login-button {
  padding: 15px;
  background-color: #1E90FF;
  color: white;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  font-size: 16px;
  transition: background-color 0.3s;
  text-decoration: none; /* Remove underline from anchor */
  flex: 1; /* Make buttons take equal space */
}

.login-button:hover {
  background-color: #0E76A8;
}

@media (max-width: 480px) {
  .login-selection {
    padding: 30px 15px; /* Adjust padding for smaller screens */
  }

  h2 {
    font-size: 1.5rem; /* Adjust font size for smaller screens */
  }

  .login-button {
    font-size: 14px; /* Adjust button font size */
  }
}

  </style>
</head>
<body>

  <div class="login-selection">
    <h2>Welcome! Please Choose Your Login Type</h2>
    <div class="button-group">
      <a href="/userLogin"style="text-decoration:none">
        <button class="login-button">User Login</button>
      </a>
      <a href="/orgLogin"style="text-decoration:none">
        <button class="login-button">Organization Login</button>
      </a>
    </div>
  </div>

</body>
</html>
