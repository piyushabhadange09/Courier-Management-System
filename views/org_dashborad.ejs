<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Organization Dashboard</title>
    <link rel="stylesheet" href="styles.css" />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Roboto", sans-serif;
        background: linear-gradient(135deg, #74ebd5, #acb6e5);
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        flex-direction: column;
        padding: 20px;
      }

      .dashboard {
        background-color: white;
        padding: 40px;
        border-radius: 15px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        width: 100%;
        max-width: 800px;
        transition: box-shadow 0.3s ease;
        overflow: hidden; /* Ensures rounded corners */
      }

      .dashboard:hover {
        box-shadow: 0 12px 24px rgba(0, 0, 0, 0.3);
      }

      h2 {
        margin-bottom: 20px;
        color: #1e90ff;
        text-align: center;
        font-size: 2rem;
        font-weight: bold;
        border-bottom: 2px solid #1e90ff; /* Underline effect */
        padding-bottom: 10px; /* Space under heading */
      }

      .info-container {
        display: grid;
        grid-template-columns: repeat(2, 1fr); /* Two equal columns */
        gap: 20px; /* Space between items */
        margin-top: 20px; /* Space above the info container */
      }

      .info {
        padding: 15px;
        border: 1px solid #ddd;
        border-radius: 10px;
        background-color: #f9f9f9;
        transition: background-color 0.3s ease, transform 0.2s ease;
        font-size: 1.1rem; /* Increased font size for better visibility */
        color: #333; /* Darker text color for contrast */
      }

      .info:hover {
        background-color: #e6f7ff;
        transform: translateY(-2px); /* Slight lift effect */
      }

      .info strong {
        color: #1e90ff; /* Blue color for strong text */
      }

      .back-link {
        text-align: center;
        margin-top: 30px;
      }

      .back-link a {
        color: #1e90ff;
        text-decoration: none;
        font-weight: bold;
        font-size: 1.1rem;
        padding: 10px 20px;
        border: 2px solid #1e90ff;
        border-radius: 5px;
        transition: background-color 0.3s, color 0.3s;
      }

      .back-link a:hover {
        background-color: #1e90ff;
        color: white;
        text-decoration: none;
      }

      /* Responsive Design */
      @media (max-width: 600px) {
        .dashboard {
          padding: 20px;
        }

        h2 {
          font-size: 1.5rem;
        }

        .info-container {
          grid-template-columns: 1fr; /* Single column on smaller screens */
        }
      }



      .navbar {
        background-color: #007bff; /* Blue background */
        padding: 10px 20px;
        margin: 30px 0px;
      }

      .navbar ul {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        align-items: center;
      }

      .navbar ul li {
        margin: 0 15px;
      }

      .navbar ul li a {
        color: white; /* White text for links */
        text-decoration: none;
        font-size: 16px;
        font-weight: bold;
      }

      .navbar ul li a:hover {
        color: #ffcc00; /* Yellow hover effect for links */
      }

      .logout-btn {
        background-color: #ff4d4d; /* Red background for the logout button */
        padding: 8px 16px;
        border-radius: 5px;
        text-transform: uppercase;
      }

      .logout-btn:hover {
        background-color: #e60000; /* Darker red on hover */
      }

      /* Ensure the logout button is pushed to the right */
      .navbar ul li:last-child {
        margin-left: auto;
      }
    </style>
  </head>
  <body>
    <div class="navbar">
      <ul>
        <li><a href="/orgLogin/sendParcel">Send Parcel</a></li>
        <li><a href="/orgLogin/deliveryStaff">Delivery Staff</a></li>
        <li><a href="/orgLogin/branches">Branches</a></li>
        <li><a href="/orgLogin/services">Services</a></li>
        <li><a href="/orgLogin/warehouse">Warehouse</a></li>
        <li style="margin-left: auto">
          <a href="/orgLogin/logout" class="logout-btn">Logout</a>
        </li>
      </ul>
    </div>
    <div class="dashboard">
      <h2>Welcome to Your <%= organization.org_name %> Dashboard</h2>
      <div class="info">
        <strong>Organization Name:</strong> <%= organization.org_name %>
      </div>
      <div class="info">
        <strong>Organization Email:</strong> <%= organization.org_email %>
      </div>
      <div class="info">
        <strong>Contact:</strong> <%= organization.org_contact %>
      </div>
      <div class="info">
        <strong>Pincode:</strong> <%= organization.org_pincode %>
      </div>
      <div class="info">
        <strong>Manager Name:</strong> <%= organization.org_manager_name %>
      </div>
      <div class="info">
        <strong>Manager Email:</strong> <%= organization.manager_email %>
      </div>
      <div class="info">
        <strong>Manager Contact:</strong> <%= organization.manager_contact %>
      </div>
    </div>
  </body>
</html>
